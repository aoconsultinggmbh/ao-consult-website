/* Echtes Kiefer-Modell einhängen.
   Sobald eine GLB-Datei vorliegt, wird sie mit  node werkzeug/modell-einbetten.js pfad/zur/datei.glb
   in diese Datei geschrieben (als Data-URI, damit sie auch per Doppelklick unter file:// lädt).
   Solange diese Datei leer bleibt, zeigt die Seite den im Code gebauten Kiefer. */
// window.AO_KIEFER = { glb: 'data:model/gltf-binary;base64,....', skalierung: 1, drehung: [0, 0, 0], versatz: [0, 0, 0], luecke: { x: -1.6, y: -0.2, z: 1.6, winkel: 0.6 }, kroneName: 'Krone', schraubeName: 'Implantat' };
